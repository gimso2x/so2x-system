---
description: Create a feature task doc and route feature work through so2x-system.
allowed-tools: Bash(python3 .so2x-system/scripts/execute.py:*),Read,Write
argument-hint: [title] [goal]
---

Run `python3 .so2x-system/scripts/execute.py flow-feature --title "<title>" --goal "<goal>"` using the user request.
Default title: `Feature task`.

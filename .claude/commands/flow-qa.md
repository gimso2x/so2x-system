---
description: Create a QA task doc and route debugging work through so2x-system.
allowed-tools: Bash(python3 .so2x-system/scripts/execute.py:*),Read,Write
argument-hint: [title] [goal]
---

Run `python3 .so2x-system/scripts/execute.py flow-qa --title "<title>" --goal "<goal>"` using the user request.
Default title: `QA task`.
